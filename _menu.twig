<header class="navbar m-2">
  <section class="navbar-section">
  </section>

  <section class="navbar-section">
    {% for item in menu %}
      <a  class="{% if ( item|current ) or ( record.contenttype.slug in item.link ) %} active{% endif %}"
          href="{{ item.link }}">
        {{ item.label }}
      </a>
    {% endfor %}
  </section>
</header>
